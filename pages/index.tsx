import type { NextPage } from "next";
import Head from "next/head";
import Image from "next/image";

import Container from "../components/Container";

//TODO: customize min-h-screen to adjust without the nav bar, idk figure it out
const Home: NextPage = () => {
  return (
    <Container>
      <div className="min-h-screen">work in progress</div>
    </Container>
  );
};

export default Home;
